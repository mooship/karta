{
  "name": "karta",
  "version": "1.0.0",
  "description": "Reusable SDK for public-interest geospatial layer platforms",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check . && npm run depcruise",
    "depcruise": "depcruise packages/*/src --config .dependency-cruiser.cjs",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "npm run test:e2e --workspace @karta/web",
    "typecheck": "npm run typecheck --workspace @karta/core && npm run typecheck --workspace @karta/app && npm run typecheck --workspace @karta/theme && npm run typecheck --workspace @karta/map && npm run typecheck --workspace @karta/react && npm run build --workspace @karta/web && npm run typecheck --prefix data-pipeline",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@swc/core": "^1.16.1",
    "@vitest/coverage-v8": "^4.1.11",
    "dependency-cruiser": "^18.2.0",
    "lefthook": "^2.1.10",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.126.0"
  }
}
