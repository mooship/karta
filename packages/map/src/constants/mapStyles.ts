/** Default stroke style for area boundary overlay lines. */
export const AREA_OUTLINE = {
  color: "#F2EDE6",
  /** Shown instead of `color` while dark theme is active (see `resolveThemedColor`). */
  darkColor: "#5b6476",
  weight: 4,
} as const;
